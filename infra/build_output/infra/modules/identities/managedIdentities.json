{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentTemplate.json#",
  "contentVersion": "1.0.0.0",
  "metadata": {
    "_generator": {
      "name": "bicep",
      "version": "0.38.5.1644",
      "templateHash": "1866942564097748811"
    }
  },
  "parameters": {
    "location": {
      "type": "string",
      "defaultValue": "[resourceGroup().location]",
      "metadata": {
        "description": "Location for the resources."
      }
    },
    "environment": {
      "type": "string",
      "allowedValues": [
        "dev",
        "sit",
        "prod"
      ],
      "metadata": {
        "description": "Environment name (dev, sit, prod)"
      }
    },
    "tags": {
      "type": "object",
      "defaultValue": {},
      "metadata": {
        "description": "Tags to apply to all resources."
      }
    },
    "userAssignedIdentities": {
      "type": "array",
      "defaultValue": [],
      "metadata": {
        "description": "Array of user-assigned managed identities to create"
      }
    }
  },
  "variables": {
    "naming": {
      "systemMI": "[format('id-system-{0}', parameters('environment'))]",
      "userMI": "[format('id-user-{0}', parameters('environment'))]"
    }
  },
  "resources": [
    {
      "type": "Microsoft.ManagedIdentity/userAssignedIdentities",
      "apiVersion": "2023-01-31",
      "name": "[variables('naming').systemMI]",
      "location": "[parameters('location')]",
      "tags": "[union(createObject('environment', parameters('environment'), 'managedBy', 'bicep'), parameters('tags'))]"
    },
    {
      "copy": {
        "name": "userAssignedMIs",
        "count": "[length(parameters('userAssignedIdentities'))]"
      },
      "type": "Microsoft.ManagedIdentity/userAssignedIdentities",
      "apiVersion": "2023-01-31",
      "name": "[format('{0}-{1}', variables('naming').userMI, parameters('userAssignedIdentities')[copyIndex()].name)]",
      "location": "[parameters('location')]",
      "tags": "[union(createObject('environment', parameters('environment'), 'managedBy', 'bicep', 'purpose', coalesce(tryGet(parameters('userAssignedIdentities')[copyIndex()], 'purpose'), 'unknown'), 'application', coalesce(tryGet(parameters('userAssignedIdentities')[copyIndex()], 'application'), 'undefined')), parameters('tags'))]"
    }
  ],
  "outputs": {
    "systemAssignedMIId": {
      "type": "string",
      "value": "[resourceId('Microsoft.ManagedIdentity/userAssignedIdentities', variables('naming').systemMI)]"
    },
    "systemAssignedMIPrincipalId": {
      "type": "string",
      "value": "[reference(resourceId('Microsoft.ManagedIdentity/userAssignedIdentities', variables('naming').systemMI), '2023-01-31').principalId]"
    },
    "userAssignedMIIds": {
      "type": "array",
      "copy": {
        "count": "[length(parameters('userAssignedIdentities'))]",
        "input": {
          "name": "[parameters('userAssignedIdentities')[copyIndex()].name]",
          "id": "[resourceId('Microsoft.ManagedIdentity/userAssignedIdentities', format('{0}-{1}', variables('naming').userMI, parameters('userAssignedIdentities')[copyIndex()].name))]",
          "principalId": "[reference(resourceId('Microsoft.ManagedIdentity/userAssignedIdentities', format('{0}-{1}', variables('naming').userMI, parameters('userAssignedIdentities')[copyIndex()].name)), '2023-01-31').principalId]"
        }
      }
    }
  }
}